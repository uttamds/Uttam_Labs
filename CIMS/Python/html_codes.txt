<!DOCTYPE html>
<html>
<body>

<h2>Fruit List</h2>

<button id="btn" onclick="showFruit()">Show Next Fruit</button>

<p id="result"></p>

<script>
let fruits = ["Apple", "Mango", "Orange"];
let index = 0;

function showFruit() {

    document.getElementById("result").innerHTML += fruits[index] + "<br>";

    // Last fruit?
    if (index == fruits.length - 1) {

        document.getElementById("result").innerHTML +=
            "<br><b>This was the last fruit.</b>";

        // Disable button for 5 seconds
        document.getElementById("btn").disabled = true;

        setTimeout(function () {
            document.getElementById("result").innerHTML = "";
            index = 0;
            document.getElementById("btn").disabled = false;
        }, 5000);

    } else {
        index++;
    }
}
</script>

</body>
</html>

================
about.html
===============

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Flask</title>

    <style>
        *{
            margin:0;
            padding:0;
            box-sizing:border-box;
            font-family:Arial, Helvetica, sans-serif;
        }

        body{
            background:linear-gradient(135deg,#4facfe,#00f2fe);
            display:flex;
            justify-content:center;
            align-items:center;
            height:100vh;
        }

        .card{
            background:white;
            width:700px;
            padding:40px;
            border-radius:15px;
            box-shadow:0 10px 30px rgba(0,0,0,0.2);
            text-align:center;
        }

        h1{
            color:#0d47a1;
            margin-bottom:20px;
            font-size:40px;
        }

        h2{
            color:#555;
            margin-bottom:20px;
        }

        p{
            color:#444;
            font-size:18px;
            line-height:1.8;
            margin-bottom:15px;
        }

        .highlight{
            color:#1976d2;
            font-weight:bold;
        }

        .btn{
            display:inline-block;
            margin-top:20px;
            padding:12px 25px;
            background:#1976d2;
            color:white;
            text-decoration:none;
            border-radius:8px;
            transition:0.3s;
        }

        .btn:hover{
            background:#0d47a1;
            transform:scale(1.05);
        }

        footer{
            margin-top:30px;
            color:#777;
            font-size:14px;
        }
    </style>
</head>

<body>

    <div class="card">

        <h1>About Flask</h1>

        <h2>Python Web Development Made Simple</h2>

        <p>
            <span class="highlight">Flask</span> is a lightweight Python web
            framework used to build websites, web applications, and REST APIs.
        </p>

        <p>
            It is easy to learn, flexible to use, and perfect for beginners as
            well as professional developers.
        </p>

        <p>
            This page is being served from the
            <strong>templates</strong> folder using
            <strong>render_template()</strong>.
        </p>

        <a href="/" class="btn">🏠 Back to Home</a>

        <footer>
            Flask Demo Project | HTML + CSS + Python
        </footer>

    </div>

</body>
</html>
